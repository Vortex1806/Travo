/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['qgetngfeghppdxtxqlds.supabase.co', 'cdn.vectorstock.com'], // Add your Supabase domain here
    },
};

export default nextConfig;
